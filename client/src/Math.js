import React from 'react';

function Math({user}) {
  // console.log(user)

  return (
    <div>
      <h2>Math Component</h2>
      {/* Add content for Math here */}
    </div>
  );
}

export default Math;
